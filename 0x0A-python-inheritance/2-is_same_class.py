#!/usr/bin/python3
"""Program to check instance"""


def is_same_class(obj, a_class):
    """
    function to check type of object
    """
    if type(obj) == a_class:
        return True
    return False
